import { CheckInputBlankDirective } from './check-input-blank.directive';

describe('CheckInputBlankDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckInputBlankDirective();
    expect(directive).toBeTruthy();
  });
});
